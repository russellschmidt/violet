<div class="container">
  <div class="row">
    <div class="col-md-8">
        <h1>Rating: <%= @rating.severity %> </h1>
        <% @rating.topics.each.do |t| %>
          <div>
            Topic: <%= link_to topic.name, topic %>
          </div>
        <% end %>
        <% @rating.posts.each do |p| %>
          <div>
            Post: <%= Link_to post.title, topic_post_path(post.topic, post) %>
          </div>
        <% end %>
    </div>
  </div>
</div>
